# plugins loader

Model (locked 2026-09-14 with the human; DDD crunch grounded in Evans Ch 1/5/6):

- `Plugin` — value object: manifest + valid skill components + valid MCP server components. Contains only valid things; everything rejected survives only as a `Report`.
- `InstalledPlugin` — entity: identity = manifest `name` (spec §9.1 keys data dirs by name); carries `plugin`, `root`, `dataDir` (= `<dataRoot>/<name>`; client-managed = freebuff-managed).
- Tiny aggregate: the manifest alone gates existence (§5.3 fatal); every other failure is skip-and-report, never fatal to siblings.
- `loadManifest` (Factory, atomic), `loadPlugin` (Service), `Report` (value object — "MUST/SHOULD report" is mandatory domain behavior).
- `extensions` carried shape-only, never interpreted (§8.1); NOT a component — §7 defines exactly two component types.
- Module layout (2026-09-14, human ruling after the SRP/screaming review of the 308-line `manifest-policy.ts`): the manifest is split one spec section per module — `plugins/load-plugin-manifest.ts` (the load use case and the §5.2 fatality order; renamed from `manifest.ts` by the F9 ruling below), `plugins/manifest/{plugin-manifest,plugin-name,metadata,schema-version,top-level-fields,extensions}.ts`, and `plugins/json-value.ts` for the JSON predicate the component rules share; `plugins/report.ts` stays innermost. Four rules come with it: a file is named for the spec section it enforces and never for its technical layer ("policy" is banned as a name — in Clean Architecture's own vocabulary it means the whole inner ring, not one file's rules); no `export` without a consumer; the manifest graph stays acyclic with the contract types as its leaf; tests mirror the split, one file per section, so no test file can drift back to the 500-line cap.
- Later phases keep this grain: `skills.ts`, `mcp-config/` (adapter), `loader.ts` at `plugins/` level; `expand.ts` moves to the next PR with Phase 4 (scope cut 2026-09-14).
- MCP stance (2026-09-14, human ruling): server shapes are validated by the existing `common/src/types/mcp.ts` schemas (`mcpConfigStdioSchema` / `mcpConfigRemoteSchema`), never re-declared. Where the spec asks for more than freebuff implements today we do **not** build it — we leave an inline note naming the § and the missing upstream capability, so the maintainers can see exactly where their own MCP support falls short of the spec and decide for themselves. Gaps to note at the seam: `cwd` (absent from `MCPConfig` and not passed to `StdioClientTransport`, so §7.2.1's default-to-plugin-root is unhonored), `PLUGIN_ROOT`/`PLUGIN_DATA` provisioning into the subprocess env (§9.1), and `${PLUGIN_ROOT}`/`${PLUGIN_DATA}` expansion in args/env/cwd (§9.2 — the existing `$VAR`→`process.env` substitution in `common/src/mcp/client.ts` is a different mechanism). Ours to enforce regardless, since they need no upstream capability: closed top level, variant exclusivity, `type` presence (the freebuff schemas default it), reserved env names (§9.2), remote URL/header rules; whether a present-but-unhonorable `cwd` gets form-validated-then-ignored or is treated as an invalid entry is T23's call.
  Test prose (2026-09-14, human ruling): every test reads as Given/When/Then — the docstring carries the GWT sentence, the title is trigger→outcome, the body is straight-line AAA; table-driven test.each only for large spec-enumerated sets (T2), never for dense object tables whose titles interpolate garbage.
  DSL rationale (2026-09-14): the rejection contract is not the boolean complement of the success contract — "not ok-true" includes "the loader crashed", which the Factory forbids; hence paired helpers expectManifestOk / expectManifestRejected (the negative one also asserts the reason blames the field under test).
  T7 merge note: fold the T4 permitted-but-unimplemented-fields row (no reports) into Test 7 when it lands — same fixture carries values verbatim + no-reports claim.
  Spec source: github.com/agentplugins/agent-plugins-spec/blob/main/spec/1.0.0.md (v1.0.0 published; 1.1.0 draft). Local cache verified byte-identical 2026-09-14.

## Scope of this PR (locked 2026-09-14, after the Phase 1 reflection)

Three deliverables, and nothing else:

1. parse `plugin.json` per the spec — Phase 1, done (T1–T11)
2. reuse the existing readers for the components: a plugin's `skills/` load through `loadSkills({ skillsPath })`, and its `mcp.json` through one thin adapter that reshapes a spec server entry into a freebuff `MCPConfig`. If the reader accepts the file, the component is accepted — no second validator, no second conformance suite
3. `freebuff plugin install <url>` for the google-cloud-developer plugin, usable after a restart — Phase 5

One exception, because item 3 is impossible without it: the existing MCP reader cannot read a spec `mcp.json` at all (its file schema is `{mcpServers}` over `mcpConfigSchema`, whose remote `type` is `enum(['http','sse'])`, against the spec's `streamable-http`), so the adapter exists to make the file readable. Server shape still comes from the freebuff schemas, never re-declared.

Assumed, not enforced — the §-deltas stay recorded as inline notes at the seam (the MCP stance ruling above still governs): skill frontmatter beyond the spec's `name`/`description`; **§7.1's SHOULD-report for a refused skill and §6.2's component-type-invalid**, where the existing reader is silent and a dropped skill therefore leaves no trace (Phase 2 records the cost); §7.2 conformance beyond what a `MCPConfig` can express; and §9.1/§9.2 env and expansion (Phase 4, deferred).

Kept regardless (ruled 2026-09-14, no upstream capability required): §4.1.1 containment at every read of a plugin-relative path — the predicate already exists and a plugin is untrusted input.

The cut's known cost: `install` can succeed on a plugin whose MCP entry we would later refuse. Mitigated for free — the adapter must run the freebuff schema to produce a config, so a bad entry is skip-and-report by construction (Test 20).

## Phase 1: manifest (Factory — common/src/plugins/load-plugin-manifest.ts)

- ✅ Test 1: loadManifest(root) on the §5.2 minimal manifest ($schema canonical 1.0.0 id, name "minimal-plugin") → ok, manifest.name === "minimal-plugin", no reports
- ✅ Test 2: §5.5 name constraints — invalid list (My-Plugin, -start, has--double, too.many..dots, empty, 65 chars) each → not ok + report naming `name`; valid list (my-plugin, acme.tools, lint3r, a) plus the 64-char inclusive edge → ok
- ✅ Test 3: §5.3 required fields — missing or wrong-typed $schema/name → not ok; no manifest object produced (Factory refuses)
- ✅ Test 4: §5.2 unknown top-level field → ok + report naming it; field NOT carried on the parsed manifest
- ✅ Test 5: §8.1 extensions — absent → undefined, no report; object → carried onto the manifest unchanged, no reports about its contents; non-object → ok + report, left undefined
- ✅ Test 6: §5.2 $schema selection — unrecognized version canonical URL → not ok + unsupported-version report; non-canonical http:// variant → not ok; no network fetch attempted
- ✅ Test 7: §5.4 metadata — content-invalid values (version "banana", homepage/repository "not a url", license "nope") → ok, carried verbatim, no reports (spec: MUST NOT reject solely on these); a field whose JSON type is wrong (version 42) → not ok (§5.2 fatality)
- ✅ Test 8: §5.4 author fatality + keywords type — unknown key, non-string value, whole-field non-object → not ok; a permitted author and keywords are carried verbatim; keywords must be an array of strings, a non-string element included → not ok
- ✅ Test 9: §5.2 manifest is JSON and a top-level object — invalid JSON → not ok; top-level array/primitive/null → not ok
- ✅ Test 10: §4.1.1 containment — a plugin.json resolving outside the root → not ok (junction fixture: this machine refuses file symlinks with EPERM, junctions need no privilege); a root reached through a reparse point with its manifest inside → ok (both sides are filesystem-resolved)
- ✅ Test 11: §5.1 absence — no plugin.json at the root → not ok, the reason naming the missing manifest, and no report (nothing was examined, and a report describes a manifest that was read); a path that is present but resolves nowhere reaches the same refusal

## Phase 2: skills (reuse the existing reader — no plugin-side skill module)

Ruled 2026-09-14 (scope cut, then narrowed again the same day after asking what the tests would prove): a plugin's skills are whatever `loadSkills({ skillsPath: <root>/skills })` returns. The reader already walks `skills/*/SKILL.md` one level deep and already skips a malformed document without losing its siblings — the spec's §6.2/§7.1 layout and the reader's discovery agree, so a second validator would only be a lookalike. Phase 2 adds exactly one thing: the containment call at the walk. Two rows, both guarding our own code rather than the reader's.

- ✅ Test 13: missing skills/ → zero skills, no error (§6.2). Guards our containment call, not the reader: `resolvesWithinRoot` throws on a missing path, so the walk absorbs the throw as absence — sabotage-proof (flipping the catch's result to a refusal turns exactly this row red)
- ✅ Test 16: §4.1.1 containment at the skill walk, both boundaries under one rule — `skills/` itself resolving outside the root → the component type is invalid (§6.2, boundary 2), and a discovered skill resolving outside the root → that skill is skipped, sibling loads (§7.1, boundary 3). Landed as `plugins/skills.ts` (`loadPluginSkills(root, readSkillsDir)`) with the reader injected — common cannot import the SDK, and the walk contributes only containment and reports, never validity. Sabotage-proof per boundary (inverting either decision turns exactly its row red). The same rule guards the `mcp.json` read in Phase 3, with no separate row

Deliberately not built — unhonored spec duties, recorded here instead (ruled 2026-09-14: a smaller PR beats a second discovery pass):

- §7.1 _"the client SHOULD report the invalid skill"_, and §6.2's component-type-invalid outcome for a `skills/` that is present but not a directory. The existing reader is silent on both — `discoverSkillsFromDirectory` catches the `readdirSync` throw and returns `{}`, `parseSkillFileContent` returns `null` outside `verbose` logging — so three spec-distinct outcomes arrive as one answer: **absent** (legal), **present-but-wrong-kind** (component type invalid), and **one refused skill** (skipped, reportable). Consequence to state in the Phase 5 render rather than paper over: `skills 5 registered` can print while a sixth was silently dropped. Honoring it needs a report-only enumeration of `skills/` diffed against the reader's output — a ledger, not a validator, since it holds no validity logic — deferred to the next PR with Test 14 and Test 15
- the reader's extra constraints can silently drop a spec-valid skill: frontmatter `name` must equal the directory name (`parse-skill.ts`), and a directory name must match the SDK's skill-name regex — which is why Test 29 asserts against the real bundle rather than against the layout
- Test 12 folded into Test 29 (ruled 2026-09-14): both assert the same five skills from the same fixture, so the path seam and the registry wiring are one row, not two

## Phase 3: MCP config (thin adapter — re-derive the sketch from the model before starting)

Adapter boundary (2026-09-14, grounded on google-cloud-developer's real `mcp.json` plus the spec text and the transport constructors in `common/src/mcp/client.ts` — not on label matching): the file is `{$schema: <version>/mcp.schema.json, mcpServers}`. §7.2's two remote variants correspond one-to-one with the freebuff `type` values, and the naming difference is a label rather than a transport: `streamable-http` ("selects the current MCP Streamable HTTP transport", §7.2.1) is freebuff's `'http'`, which builds `StreamableHTTPClientTransport`; `sse` ("the deprecated HTTP+SSE transport defined by the MCP 2024-11-05 specification", §7.2.1) is freebuff's `'sse'`, which builds `SSEClientTransport`. Neither side defines a third remote transport. So the adapter is a rename that has to be _documented_ rather than inferred, and the check that backs it is the constructor in `common/src/mcp/client.ts` — a label match alone would keep passing if freebuff's `'http'` ever stopped building the Streamable HTTP transport, silently connecting the plugin the wrong way. The freebuff `type` also carries a zod default where §7.2 requires the field. Mapping and §-cited reporting are ours; `$schema`-vs-manifest-version selection is deferred with Test 19; the server _shape_ is freebuff's own.

- ✅ Test 21: adapter maps a spec server entry onto a freebuff `MCPConfig` — `streamable-http` → `'http'`, `sse` → `'sse'`, `$schema` not carried. **Dropped as its own row after review (2026-09-14):** T26 loads the same fixture and asserts the full freebuff shape with `toEqual`, which subsumes the mapping and the `$schema`-not-carried claim (the sabotage already showed the mapping swap turns both rows red, so T26 alone catches it), and the spec offers no example implementation to test against anyway — the real bundle gets exercised live in the install phase instead. The mapping's _reason_ stays recorded here: freebuff's `'http'` is the Streamable HTTP transport per its constructor in `client.ts`, not a separate plain-HTTP variant
- ✅ Test 18: missing mcp.json → no error, no report (nothing was read); present but not a regular file → §6.2 component-type-invalid, carried as an error report while the plugin still loads (landed stronger than sketched: the walk is ours, unlike skills, so the reporting duty is honored — see the Phase 2 silence note)
- ✅ Test 20: one server entry the freebuff schema refuses → skipped + report, sibling servers load (also proved the adapter never crashes on a bad entry)
- ✅ Test 26: only mcp.json loads — the root is built with both real decoys present (`mcp_config.json` is the Gemini CLI extension shape, `serverUrl`/`authProviderType`); loading yields exactly `developer-knowledge` as freebuff `http` and zero reports about either decoy
- ✅ (added during the audit) an mcp.json resolving outside the root → §4.1.1 refusal: the tracker's "no separate row" note assumed Phase 3 would reuse the skills walk; the adapter has its own read path, so its containment wiring earns its own proof — and the first sabotage run showed the check was ordered after the kind classification, which let an escaping junction hide behind the wrong-kind branch. §4.1.1 now runs first, on the resolved path

Deferred to seam notes, not built in this PR (2026-09-14 scope cut) — §7.2's per-entry rules: Test 19 (invalid JSON and an unrecognized `$schema` stay readability failures the adapter reports; version-vs-manifest is §5.2's job, already Test 6), Test 17's closed top level and variant exclusivity, Test 22 (bare-token command, no `../`), Test 23 (cwd forms — this also retires the earlier "present-but-unhonorable `cwd`" judgment call), Test 24 (remote URL and header rules), Test 25 (§9.2 reserved env names). Two consequences worth recording for the maintainers: the freebuff remote shape is `url: z.string()` with no URL check, and nothing enforces the reserved env names — neither is reachable on this plugin's happy path, whose only server is remote.

## Phase 4: env & expansion (deferred to the next PR — 2026-09-14 scope cut)

Nothing in this PR's happy path reads it: google-cloud-developer's only server is remote `streamable-http`, so no subprocess is spawned and no placeholder appears in its `mcp.json`. The §9.1/§9.2 gaps stay recorded at the seam (the earlier list stands: `cwd` absent from `MCPConfig` and not passed to `StdioClientTransport`; `PLUGIN_ROOT`/`PLUGIN_DATA` not provisioned into a subprocess env; expansion is a different mechanism from the `$VAR`→`process.env` substitution in `common/src/mcp/client.ts`).

- Test 27 and Test 28 move to the next PR unchanged (placeholder expansion in args/env/cwd; the subprocess env overlay and `dataDir` contents surviving an update). Install still provisions the dir — `<dataRoot>/<name>` is one `mkdir` on the entity's own path and gives Test 33's "nothing written on failure" a real target — but nothing reads it until expansion lands.

## Phase 5: session surface & install (clarified 2026-09-14 with the human)

Goal, in the human's words: `freebuff plugin install https://github.com/google/skills/plugins/cloud/google-cloud-developer` installs the skills + MCP server bundle that ships with that plugin; after a freebuff restart, a Google-Cloud question reaches the right skill and the right MCP server.

Command surface: commander (`cli/src/cli-args.ts`; the Freebuff branch declares one subcommand today — `[command]` choices `['login']`). `plugin install <url>` extends that surface as a nested commander command; `--cwd`/`--continue` untouched. In-session `/plugin` is out of scope for the merge shield unless the human wants it.

Install source (researched and decided 2026-09-14): **HTTPS tarball, no git binary, no new dependency.** `fetch('https://codeload.github.com/<o>/<r>/tar.gz/<ref>')` → `new Bun.Archive(blob)` → `extract(tmp, { glob: ['*/<subpath>/**'] })`. `Bun.Archive` is in the _pinned_ runtime, not just locally: `.bun-version` is 1.3.14 and the API landed in Bun 1.3.6. Proved against the real target before writing it down — `HEAD` → 200, `application/x-gzip`, **1.45 MB**, and `archive.files('*/plugins/cloud/google-cloud-developer/**')` enumerates exactly the plugin's 18 files (7 root files, `rules/`, 5 skills of which 2 ship `references/`). codeload is not the REST API, so no 60 req/hour limit and no auth; public repos only, per the human's ruling. Path safety is built in — absolute/drive/UNC paths rejected, `..` normalized, unsafe symlink targets dropped — which matters because this is untrusted input.
How the field does it (survey 2026-09-14, for the record): Claude Code ships **both** — `git-subdir` ("clones sparsely to minimize bandwidth for monorepos") _and_ `archive` ("zip archive downloaded over HTTPS. Works without git or npm on the user's machine") — while Gemini CLI clones by default and recommends GitHub Releases archives precisely to _avoid_ a repository clone. The one git-shaped example is `npx skills add` (vercel-labs/skills) — google's own recommended command — which uses `simple-git`; that is a Node tool with no built-in tar, i.e. a reason, not a requirement. Rejected: `git` sparse clone (needs a git binary for something the runtime already does), GitHub contents API (60 req/hour), GitHub Releases (google/skills publishes none).
URL forms: repo root (`<url>`, trailing `.git`) and subdirectory (`…/<path>`, `…/tree/<ref>/<path>`); no ref = `HEAD`, which codeload resolves to the default branch. Configurable `<pluginsRoot>` via env so tests never touch the real `~/.agents`.
Ref/identity: the archive's top-level directory is `<repo>-<ref>`, so strip the first path segment rather than matching a literal name, and record the source URL + ref with the install (no commit sha is available this way; sha pinning would need the API, i.e. rate limits — future work).
Install order: fetch into a temp dir → `loadManifest` → **conflict check** → move to `<pluginsRoot>/<name>`; any failure → report, temp removed, nothing registered.
Conflict policy (human ruling 2026-09-14): if the plugin name is already installed, or any of its skill names or MCP server names already exist in the user's roots, **abort** — install never shadows, never merges, and cannot be half-applied. Worth knowing before the live run: `google-cloud-recipe-auth` is already in this machine's `~/.agents/skills` from the 2026-09-13 probe, so the e2e either starts from a clean roots fixture or asserts the abort — the second is a good T32 case either way.

Plugins root: `~/.agents/plugins/<name>`, the home root by default (human ruling 2026-09-14: every coding agent in the field keeps skills and MCP servers in the user's home `.agents`, and `~/.agents/plugins` is also the spec's own §9.1 example, beside the `~/.agents/skills` and `~/.agents/mcp.json` roots freebuff already reads). Installing into the project's `{project}/.agents` is a **future PR** — a scope flag, not this one. Data dir: `~/.agents/plugins/.data/<name>`, and the constant **must carry a comment** (human ruling) explaining that the dot is deliberate: §5.5 names must start alphanumeric, so `.data` can never collide with a plugin root, whereas a literal `data/` sibling could be a plugin _named_ `data` — we decline to rely on "nobody will ever name their plugin `data`", however remote that chance is (§9.1 leaves the data location to the client). Discovery reads exactly `<pluginsRoot>/*/plugin.json` — one level, no recursion.

Session surface (what "restart and ask" needs):

- skills: `initializeSkillRegistry()` (`cli/src/utils/skill-registry.ts`) makes one `sdkLoadSkills({cwd, includeHomeSkills:true})` call today; plugin roots become extra `loadSkills({skillsPath: <root>/skills})` calls merged into the cache — SDK untouched, which is the merge shield. No precedence rule is needed (the question is retired): install aborts on a name conflict, so a plugin skill can never compete with the user's or the project's.
- MCP: `local-agent-registry.ts` loads the user's `.agents/mcp.json` into `mcpServersCache` (:75) and merges it into every local agent def (:365); plugin servers join that cache as adapter-produced `MCPConfig`s, which is how `developer-knowledge` becomes reachable by every local agent. No shadowing is possible for the same reason — the conflict check runs before anything is written. The existing loader cannot be pointed at the plugin file: its schema is `z.object({mcpServers})` over the freebuff `mcpConfigSchema`, so a spec `$schema` key is stripped and `type: "streamable-http"` fails the enum → the whole file is rejected.
- persistence comes free: install runs in its own CLI process and exits, so the next start re-reads the roots — no in-process refresh, matching the labnote's long-lived-process caveat.

End-state target (grounded by fetching google/skills@main, not remembered): `plugin.json` (google-cloud-developer 1.1.2, Apache-2.0, author `{name,url}`, keywords[4]), 5 skills (finding-google-skills, gcloud, google-cloud-recipe-auth, google-cloud-recipe-onboarding, retrieving-developer-knowledge), 1 server (`developer-knowledge`, `streamable-http` → https://developerknowledge.googleapis.com/mcp). The same root carries `.claude-plugin/`, `.codex-plugin/`, `.mcp.json`, `mcp_config.json`, `gemini-extension.json`, `rules/` — all silently ignored, **zero reports**: a foreign plugin's extra files are invisible, not noise.
Proposed render: `✔ google-cloud-developer 1.1.2 ← github.com/google/skills/plugins/cloud/google-cloud-developer → <pluginsRoot>/google-cloud-developer`, then `skills 5 registered`, `mcp 1 server developer-knowledge (streamable-http)`, `data <pluginsRoot>/.data/google-cloud-developer (created)`, and a reports line that renders even when empty.
Honest limit to state in the test rather than paper over: `developer-knowledge` needs GCP credentials (the plugin's Gemini manifest declares `authProviderType: google_credentials`), so the live assertion is "configured and the connection is attempted", not "tools listed".

- ✅ Test 29: plugin skills join the session registry as a third root — the plugin root's `skills/` is the path handed to the reader, the 5 google-cloud-developer skills are present in `getLoadedSkills()`, and the user's and project's own roots are untouched (subsumes Test 12: the seam and the wiring are one assertion)
- ✅ Test 30: plugin MCP servers join the session server map — `developer-knowledge` present as freebuff `http` in `getLoadedMCPServers()`, adapter output accepted by `mcpConfigSchema`
- ✅ Test 31: install URL forms → (repo, subpath, ref): repo root, trailing `.git`, `/tree/<ref>/<path>`, plain subpath, default `HEAD`; a non-GitHub host or an ssh remote is rejected with a clear report (pure parse, no network, no `git`)
- ✅ Test 32: `freebuff plugin install <url>` on the google-cloud-developer URL — tarball fetch of the subdirectory (no git binary), lands at `<pluginsRoot>/google-cloud-developer` in a clean roots fixture, exit 0, renders the result above with zero reports. Verification path amended 2026-09-15: no tmux on this Windows machine — the command runs before the TUI renders (login/publish shape), so it is verified as a plain console command (stdout + exit code); tmux remains the path for the Unix maintainers.
- ✅ Test 33: install failures all abort cleanly — unreachable URL, 404, no plugin.json at the subpath, invalid manifest, plugin name already installed, and a plugin skill or server name colliding with the user's roots → clear report and **nothing** written (no plugin dir, no skills registered, no servers, no data dir, temp removed)
- ✅ Test 34: a separate bun child process discovers what install wrote — 5 skills + 1 server in the child's caches, no in-process refresh assumed. Verification path amended 2026-09-15: on Windows the child runs the real registry startup directly (the same calls `index.tsx` makes before rendering); tmux remains the path for the Unix maintainers.
- ✅ Test 35: the payoff — the human runs the live smoke by hand (no automated floor; every seam is unit-proven, see the outcome note). Script, Windows Git Bash:
  1. Pre-flight (should already hold): `ls ~/.agents/skills/` shows the 8 deliberate skills and NO `google-cloud-recipe-auth` (deleted 2026-09-15 — the plugin carries it; recoverable by reinstall); `ls ~/.agents/` shows no `plugins` dir; `cli/.env` exists (the `fb` shim hard-requires it).
  2. Install against the real roots: `fb plugin install https://github.com/google/skills/plugins/cloud/google-cloud-developer` → expect `✔ google-cloud-developer 1.1.2`, `skills 5 registered`, `mcp 1 server: developer-knowledge`, a data-dir line, exit 0, **no collision abort** (roots are clean).
  3. Launch `fb` and ask: `how do I authenticate to Google Cloud from my app?` — should naturally pull in `google-cloud-recipe-auth`.
  4. What green looks like:
     - Startup: `Loaded 13 skills` (8 own + 5 plugin; exact number depends on project skills in cwd — run from a neutral dir).
     - The skill tool serves real content when the model invokes `google-cloud-recipe-auth` — NOT `Skill 'google-cloud-recipe-auth' not found` (that failure is the `skillsLoader` gap `aa15638aa` closed).
     - `developer-knowledge` connection attempted through the real MCP client; failing on GCP credentials is the expected, passing floor.
     - Bonus: `/` slash menu lists the plugin skills (same cache feeds both surfaces — `aa15638aa` made them one source).
  5. Also worth one look: `ls ~/.agents/plugins/google-cloud-developer/` shows the plugin dir with `skills/`, `mcp.json`, `.data/`, and the foreign files carried silently (`.claude-plugin/`, `.mcp.json`, `mcp_config.json`, `gemini-extension.json`).
  6. After the smoke: report the results back for the tracker row. Uninstall path (when wanted): remove `~/.agents/plugins/google-cloud-developer` — the 5 plugin skills vanish from the next session's startup load; `google-cloud-recipe-auth` returns to its pre-plugin absence unless reinstalled.
- **Test 32 outcome** (2026-09-15, `9e8727773`):
  - Landed: `runPluginCommand` + `runPluginInstallCommand` in `plugin-install-command.ts` (command layer: usage rows, the render, `userSkillNames()`/`userMcpServerNames()` reading the same skill/MCP roots the session reads), `plugin-install.ts` slimmed back to the value-returning pipeline, `cli-args.ts` gains the `plugin` choice + excess-arg allowance, `index.tsx` dispatches before the TUI renders (login/publish shape). Live-proven against the real binary: happy path `✔ google-cloud-developer 1.1.2, skills 5, mcp 1, zero reports, rc=0`; re-run aborts `already installed, rc=1`; real roots abort `skill name already in use: google-cloud-recipe-auth, rc=1` (the real home untouched).
  - The live probe caught what the unit rows could not: the dispatch originally forwarded empty name-sets, so the conflict check ran against nothing on the real path. Fixed in the command layer; the real-root abort is now the standing proof.
  - Human challenge (2 catches), both upheld by re-audit: (1) the file had grown to 338 lines / three actors (pipeline, console render, real-roots assembly) — split into two modules per clean-arch SRP, and the spread-after-defaults fragility in the dispatch removed by defaulting seams before applying caller options; (2) the command test had not been walked against the pruning rules — deleted the extra-args row (input hoarding, same guard branch as missing-URL), trimmed the 404 row's readdir assertion (owned by the pipeline suite's abort rows), deleted dead helpers. 6 rows → 5.
  - Audit (17 stages fresh, post-split): cc/ca/tr pass; subcommand-guard sabotage re-proven on the split module; internal-reference sweep clean.
- **Test 29 outcome** (2026-09-15, `be2a848ce`):
  - Landed: `cli/src/utils/plugins-root.ts` (the shared root: `FREEBUFF_PLUGINS_ROOT` override, absolute-only, mirroring `FREEBUFF_CONFIG_DIR`; `plugin-install.ts` uses it too), `cli/src/utils/plugin-discovery.ts` (one readdir of `<pluginsRoot>/*`, dot-dirs excluded, `loadPlugin` per dir; unreadable root → empty; exports `pluginSkills()` — the merged plugin-half map the registry assigns over its cache).
  - Conflict hygiene (human ruling after catching the first draft): the first draft rewrote `initializeSkillRegistry`'s body in the upstream-owned `skill-registry.ts` — the divergence ledger's V1 mistake (`logger.debug` pass, dropped on V2 for exactly this). Restructured per the ledger's rule "new code in its own module, so future rebases stay small": the upstream file's delta is now 3 functional lines (import, docstring pointer, the one `Object.assign` join) plus a prettier-forced wrap. Our logic — reader adapter, merge loop, injection seam — lives in `plugin-discovery.ts`; the test drives `pluginSkills()` directly.
  - 3 rows: wiring+exact path (`<pluginRoot>/skills` handed to the reader), missing root contributes nothing, decoys contribute nothing. Sabotage: removing the dot-filter turns exactly the decoy row red.
  - Audit (17 stages fresh): cc/ca/tr pass. Findings fixed in-flight: (1) an inline comment narrating what the interface docstring already stated — dropped (comment doctrine); (2) duplicated `import type` lines — merged; (3) the decoy row originally parked a valid plugin _inside_ `.data`, which cannot prove the filter — without it, `loadPlugin('.data')` fails at the manifest before the reader is reached. The tripwire is a valid plugin directly in a dot-directory. Also caught by tsc: the production reader initially wrapped the async `sdkLoadSkills` against the sync seam — `loadSkillsSync` is the adapter, as in the installer.
  - Out of scope for the unit rows, proven live later: the real-SDK path against a real install is T34/T35.
- **Test 30 outcome** (2026-09-15, `5fa7d563b`):
  - Landed: `pluginMcpServers()` beside `pluginSkills()` in `plugin-discovery.ts` (same scan; no skill reader needed — MCP config is loaded by common, so this never touches the SDK; discovery's reader param became optional with a no-op default). Upstream `local-agent-registry.ts` delta: one import + one `Object.assign` join after the mcp.json try/catch — a broken user mcp.json must not hide plugin servers (comment carries that why). Conflict hygiene applied from the start this time; internal-reference sweep clean.
  - 3 rows: spec `streamable-http` arrives as valid freebuff `http` (parsed through `mcpConfigSchema`, defaults `params`/`headers` present per the adapter's documented contract), servers from two plugins merge into one map, missing root contributes nothing. Sabotage: swapping the adapter's transport mapping (`http`→`sse`) turns exactly the shape row red.
  - Test bugs caught on the way (both mine): expected the bare shape and missed the adapter's emitted defaults; used `type: 'http'` in a fixture, which the spec doesn't allow — the adapter refused it, accidentally proving the refusal row.
  - Audit (17 stages, fresh this session): cc/ca/tr pass. Pruning deliberation recorded: the absence row duplicates T29's shape-for-shape, but each pins a separately exported function's contract (absence → empty map, not throw) through the shared discovery; kept as cheap symmetric pinning.
  - Live end-to-end (cache → base-agent merge → real connection attempt) is T34/T35.
- **Test 35 outcome** (2026-09-15, live smoke via `fb`; full report lives in the browser-automata workspace as `t35-smoke-report.md`, external to this repo):
  - PASS with the one predicted limitation. Install: `✔ google-cloud-developer 1.1.2`, 5 skills, 1 server, data dir, no collision, exit 0. Skills: `google-cloud-recipe-auth` served its full content on invocation; own roots untouched (`~/.agents/skills/` still exactly the 8 deliberate skills). MCP: `developer-knowledge` loaded as freebuff `http`, connection attempted, Google answered `UNAUTHENTICATED` — the expected floor, and §7.2.2 rule 5 held live: an authorization failure is a connection failure for that server, never invalid plugin config; the skills rode on untouched.
  - Why MCP needs auth (the report's diagnosis): spec v1.0.0 §7.2.1 — "Agent Plugins v1 defines no OAuth configuration or portable credential-reference fields"; auth is client-managed by design. Google ships it in vendor-specific files (`mcp_config.json` / `gemini-extension.json`, `authProviderType: google_credentials`) read by Gemini CLI and Antigravity — not a spec deviation, and not ours to read in this PR. Dead end ruled out: Google's remote MCP servers don't support Dynamic Client Registration, so the MCP SDK's OAuth flow cannot negotiate with `developerknowledge.googleapis.com` at all. Working fallback (used live): the Developer Knowledge REST API with `gcloud` credentials and a quota-project header.
  - New finding, verified in code: the T30 join in `local-agent-registry.ts` assigns plugin servers AFTER the user's, so a plugin server shadows a same-name entry the user later adds to their own mcp.json — install's conflict check only saw names at install time. The join's comment ("never shadows one of the user's") overclaimed: true at install time, false over time. Not hypothetical — the report's header-override fallback lost to exactly this ordering. The same hazard exists on the skills side: the registry merge is plugin-last there too, so a plugin skill wins over a same-name skill added to the user's roots after install.
  - Ruling (human, 2026-09-15): collision precedence is a product-UX decision for the maintainers, not this PR — the PR is already large, and the comments make the current behavior explicit enough for them to rule on. Deferred. The three overclaiming comments (MCP join, `pluginSkills` docstring, `pluginMcpServers` docstring) are corrected to state the actual plugin-last join and flag the open question; the behavior itself is left exactly as implemented. Candidate follow-up: user-roots-win precedence (order-independent, the convention in every config system), possibly with a startup warning on a live collision — with a probe-based collision row as its proof, since the production merge order is not unit-reachable through the injection seam.
  - SRP arc (human challenges, landed 2026-09-16, `b4a149106`): the challenge "why would an MCP walk take skill options?" traced past the first fix (each caller resolves its own reader) to the root cause — `loadInstalledPlugins` forced the whole-entity loader (`loadPlugin`: manifest + skills + MCP together, the right shape for install) onto both session walks, and its empty-reader default silently served the production skills walk zero plugins (caught only by the T34 child-process row; every unit row injects a reader). Final shape: `installedPluginRoots` enumerates directories only, each walk composes its manifest gate + one component, the MCP walk's options carry nothing skill-shaped (ISP), no default lives below any caller. Test rows pass explicit readers; the no-op-reader row pins the behavior the MCP walk's composition relies on. Comments trimmed to the load-bearing whys on the same review (comment doctrine). The tracker's 2026-09-15 CRP note ("discovery imports the composite load-plugin, so a skills-only registry init also reads mcp.json") is retired by this change.
  - Ruling (human, 2026-09-16): **bare-bones feature freeze.** The PR stays at the minimum viable plugin: one that carries multiple skills and multiple MCP servers. Multi-MCP is the spec's own shape (§7.2 `mcpServers` is a map) and is already plural all the way down — loader (per-entry map), install conflict check (iterates every server name), render (`mcp N servers`), session registry (merges the whole map) — so the only addition was the missing loader row pinning two conforming entries both arriving. The merge/shadow logic stays as implemented; **no fix for plugin-vs-plugin name reuse** (plugin B installing a server or skill name plugin A already claimed aborts on nothing — install's conflict check only sees the user's roots, and the later-loading plugin silently wins the join). Worth raising with the maintainers if they adopt the design, alongside the 2026-09-15 precedence ruling: extend the conflict sets with names already claimed by installed plugins (`Object.keys(pluginSkills())` / `pluginMcpServers()` at the command layer — a two-line change), or accept plugin-last-wins as the documented behavior. Not built here; recorded for that discussion.
  - Future-work note from the report (next PR, not this one): implicit ADC for `*.googleapis.com` MCP endpoints via the MCP SDK's `authProvider` seam in `common/src/mcp/client.ts` (~50–60 lines + `google-auth-library`; settings-flag escape hatch; sketch and rejected alternatives in the report).
  - Not captured: the startup `Loaded 13 skills` banner (the TUI wasn't under observation) — registry content proven via invocation instead.
- **Test 34 outcome** (2026-09-15, `22e71d6b5`):
  - Landed: `cli/src/utils/__tests__/child-process-probe.ts` (the child: the real registry startup — the same `initializeSkillRegistry()` / `initializeAgentRegistry()` calls `index.tsx` makes before rendering — against fixture roots from the environment, printing one JSON line with what the caches hold) and `plugin-child-process.test.ts` (spawns the probe per row, asserts against the child's report).
  - 2 rows: an installed plugin's 2 skills + server (in the freebuff shape) join the child's caches alongside the project's own skill; a machine with no plugins root starts clean and nothing throws.
  - Two test-infra catches: (1) bun's default 5s timeout killed the child spawn under suite contention (`probe failed (143)` = SIGTERM) → explicit 30s per-row timeouts; (2) Windows EBUSY on cleanup — the child is killed before `afterEach`, and the probe's last live line is its JSON so it never lingers.
  - Naming precision (human catch): "fresh process" never said _which_ process — renamed the files (`fresh-process-probe.ts` → `child-process-probe.ts`, `plugin-fresh-process.test.ts` → `plugin-child-process.test.ts`) and reworded every mention: a separate bun child process, whose module-level caches start empty because module state does not cross process boundaries.
  - Audit: cc/ca/tr pass. The internal-reference sweep caught "The T34 probe" in the probe's docstring — the leak lesson applies to test scaffolding too; a `tsc` narrowing error in the server report was fixed (`'type' in config` is always true across variants — the discriminant is `'url' in config`).
  - Checkout note (for the PR description): upstream's own e2e scaffolding is unusable from a public checkout — `test-utils.ts` requires `packages/internal/src/env`, which the public tree does not carry (~80 pre-existing unit failures funnel through that one require; `e2e-cli` skips on `!isSDKBuilt()`, tmux skips on availability). Our verification path is self-contained: fixture HOME + `FREEBUFF_PLUGINS_ROOT`, no env loader, no built SDK, no tmux.
- **Integration gap found by inspection during the T35 scoping** (2026-09-15, `aa15638aa`):
  - The gap: the session's skills never came from the CLI registry. `handleSkill` (agent-runtime) reads `fileContext.skills`, which the SDK loads itself per run (`sdk/src/run-state.ts` `loadSkills()`) from project/home dirs only — the plugins root was invisible to it. The registry cache (`getLoadedSkills()`) fed exactly one consumer: `chat.tsx`'s slash-command list. Net effect: a plugin skill would appear in slash autocomplete yet fail `Skill 'x' not found` when the model invoked it.
  - The fix: upstream already ships the seam — `RunOptions.skillsLoader?: () => Promise<SkillsMap>` (`sdk/src/run.ts`), unused by the CLI. `create-run-config.ts` now points it at the registry cache (one import + one property; upstream-owned file stays hygiene-sized). The cache is the CLI's single merged source (project + home + installed plugins), so the model's skill list and the slash-command list can no longer disagree.
  - Found by tracing, not by any e2e infra — prompted by the human's challenge that the automated T35 floor wasn't worth building. The unit row pins it: seed `__setSkillsForTests`, build a config, await `config.skillsLoader()`, assert the seeded skill arrives; red pre-join (`skillsLoader` undefined), sabotage-proven (line removed → exactly that row red).
  - `sponsored-run.ts`'s own `client.run` deliberately gets no join: the sponsored ad run is unattended and skills-less by design (`agentDefinitions: []`, bare config).
  - Automated T35 floor ruled out (human decision): with this join, every seam is proven by existing tests — registry cache (T34's child-process probe) → run config (the new row) → `handleSkill` reading `fileContext.skills` (upstream's own skill.test.ts). The live-session behavior is T35's manual smoke via `fb` (the source-mode shim — the release binary `freebuff` carries none of this branch's work; see labnote 2026-09-03-01 for the shim and its env traps).

## Phase 6: refactorings (scheduled by the T5 audit, 2026-09-14)

- ✅ F2 immutable report assembly — `reports.push(...)` mutated a local array in `loadManifest` (clean-arch S1.4, clean-code S4.3)
- ✅ F5 `isPlainObject` extraction — the typeof/null/array check appeared twice and forced two `as Record<string, unknown>` casts (clean-code S2.1, S2.3)
- ✅ F3 composed report assertion — `non-object extensions is reported and ignored` (`__tests__/extensions.test.ts`) runs 14 non-blank lines against the 10-line cap, and three report rows across `extensions.test.ts` and `top-level-fields.test.ts` repeat the section+message probe (test-review S1.4, clean-code S5.2) → one `expectReportAbout(reports, section, field)` helper in the fixtures, beside the paired load-result helpers; landed 2026-09-15 (`c47a1bdc3`), 61/61 plugins suite green
- ✅ F4 contract types moved inward — the manifest contract is now `manifest/plugin-manifest.ts`, the leaf of the graph that every rule module imports, with `PluginReport` in its own `report.ts` (every component produces them) and the I/O module importing both; no edge points outward any more (drove: the rules module imported its contract from the I/O module — clean-arch S3.1 DIP, S4.1 ADP, S4.2 SDP). `LoadManifestResult` stayed in the use case module (`manifest.ts` then; `load-plugin-manifest.ts` after the F9 rename): it is the load use case's own result and only consumes inward types
- ✅ F6 temp plugin roots are removed per test — `makePluginRoot` registers each root and an `afterEach` drains the registry with `rmSync(root, { recursive: true, force: true })`; verified 0 left after a full run (drove: `makePluginRoot` recreated a dir per test but discarded none — test-review S4.1). 597 dirs left over before the fix were deleted
- ✅ F7 report-order overspecification — `each unknown field gets its own report` (`__tests__/top-level-fields.test.ts`) asserted `reports[0]`/`[1]`, an order §5.2 does not mandate (test-review S5.2) → per-field presence via the F3 helper, keeping both strength pins (the count of two, and per-field presence — sabotage-proven: collapsing the emitter to one combined report turns exactly this row red); landed 2026-09-15 (`d021a21f3`)
- ✅ F1 uncovered refusal paths — §5.1 no manifest, §5.2 non-JSON, §5.2 non-object top level had no tests (test-review S5.1) → all three are covered now: Test 9 took §5.2's parse (`__tests__/load-plugin-manifest.test.ts`) and its non-object top level (`__tests__/top-level-fields.test.ts`), Test 11 took the §5.1 absence
- ✅ F8 test file over the 500-line cap — T8 pushed the use-case test file to 501 (from 485; it is `__tests__/load-plugin-manifest.test.ts` now), so the fixture DSL moved to `__tests__/manifest-fixtures.ts` (144) leaving 398, and the per-section split that followed moved the rows on into six files, none above 144 (drove: clean-code S1.1 hard cap; also cleared S1.4 density and S2.4 inline literals in the same move)
- ✅ F9 `manifest.ts` (the load use case) sat beside `manifest/` (its rules) — the only file-shadows-directory shape in the monorepo (swept 2026-09-15: five `index.ts` barrels repo-wide and all at boundaries — `sdk/src/index.ts`, `sdk/src/tools/index.ts`, `common/src/constants/index.ts` (a backwards-compat shim), `testing/` + `testing/mocks/`; zero in cli; none inside a feature directory — and `foo.ts` beside `foo/` occurs nowhere else). The original note claimed adding an `index.ts` would silently re-point every relative import: **disproved by probe** (with both the file and `manifest/index.ts` present, bun's runtime and tsc's bundler resolution both pick the file; precedence is lost only if the file is deleted, and node10 — the one older algorithm with different quirks — is removed in TS 5). The residual risk was two-step and cold: a barrel added now, then the file renamed later, turning a loud compile error into a silent re-bind. Resolved by renaming to `load-plugin-manifest.ts` (2026-09-15): the name states the behavior, it mirrors `load-plugin.ts`, and it removes the repo's only shadow shape. All 9 import sites name the file explicitly (1 production, 7 test files, 1 fixtures type import); the use-case test file renamed to mirror (`load-plugin-manifest.test.ts`)
- ✅ F10 fixtures grew unbounded — `manifest-fixtures.ts` reached 350 lines (144 → 226 → 350 across three phases) by accepting every component's fixtures into one module; the name no longer described its contents (skills fixtures in a file called _manifest_-fixtures). Split by component into `__tests__/fixtures/{temp-roots,manifest,skills}.ts`, layering core ← manifest ← skills; Phase 3's mcp fixtures land in their own file. Two latent fixture bugs surfaced in the move and were fixed: the escaping-manifest junction targeted a _file_ (dangles on Windows — the fixture now targets the outside directory and documents why), and one junction had link/target swapped (drove: clean-arch Ch 13 CCP, three actors one module; clean-code S1.1 trajectory)

## Full per-file audit (2026-09-15, human-directed)

Method: all three checklists read fresh (clean-code 6 stages, clean-architecture 5, test-review 6), then every new plugin-support file read in full and walked against the applicable stages — production first, tests second. Scope: the 18 new production files and 21 new test/fixture files of the plugin support, against `origin/main`; the `debug-toolbox`, `agent-runtime` and `llm-providers` changes in the same diff belong to the debug-infra lineage and are out of scope here. Stated limitations: no cyclomatic-complexity tool exists in this environment, so complexity was judged by inspection; per-row sabotage proofs were not re-run (the per-test audits recorded them as each test landed).

Fixed 2026-09-16 — one commit per finding, each gated on a green run before it landed (11 genuine; the ruling graded 3 as nitpicks — P2, P5, T6 — left unfixed):

- P1 `cli/src/utils/plugin-discovery.ts` — `pluginSkills`/`pluginMcpServers` accumulate with `Object.assign` into a mutable local inside a loop, the exact shape F2 removed from the loader and the T13/T16 fix removed from the skills walk (clean-code S4.3 purity, clean-arch S1.4 immutability) → immutable merge — fixed (`f83c1754e`): one `loadedPlugins` helper unwraps the loaded plugins, each walk merges in a single expression. **The fix first shipped a real bug the T34 child-process row caught**: passing the optional reader through let `loadInstalledPlugins`'s empty-reader fallback (`readSkillsDir ?? (() => ({}))`) fire on the production path, so the session read zero plugin skills — every unit row injects a reader, only the child-process row exercises the default; fixed in the same commit (`pluginSkills` resolves its reader before the call)
- P2 `cli/src/utils/plugin-discovery.ts` — `pluginMcpServers` takes an inline anonymous `{ pluginsRoot?: string }` where its sibling takes the named `PluginSkillsOptions` (clean-code S3.4, consistency) → one named options type
- P3 `cli/src/commands/plugin-install.ts` — the move-and-provision block (`existsSync` → `cpSync` → `rmSync` → `tempDir = undefined` → `mkdirSync`) runs inline at the orchestrator's level, with the mutable `tempDir` doubling as the cleanup flag (clean-code S2.1) → a named step that returns the destination — fixed (`f4762aa93`): `installPluginAt` does the move and the data-dir mkdir, and the `finally` is the temp dir's only reader
- P4 `cli/src/commands/plugin-install-command.ts` — `runPluginInstallCommand` mixes argument validation, seam assembly and a ~25-line render, with `process.exitCode = 1; return` repeated three times (clean-code S2.1) → extract the render and the failure branch — fixed (`f5faf0af3`): the render is `renderInstalled`, the two usage exits share `failWithUsage`
- P5 `common/src/plugins/mcp-config.ts` — 251 lines, the largest file we add: above the ~200 target, under the 500 cap (clean-code S1.1) → observed, split only if it grows
- Considered and kept, with the reasoning on record: `install-url.ts`'s guard comment explains a why the code cannot express; `schema-version.ts` is the version seam by design (multi-version support needs per-version rule sets, not a bigger table); the `node:fs` imports in `containment.ts`, `skills.ts`, `mcp-config.ts` and `load-plugin-manifest.ts` are covered by the recorded Stage 3 override and the Humble-Object rationale; the data-carrier interfaces carry no `readonly` because the codebase's existing carriers (`MCPConfig`, `SkillDefinition`) do not either — consistency over novelty
- T1 missing paired result helpers: inline `if (!ok)` branches in test bodies — `load-plugin.test.ts` (3 rows), `plugin-install.test.ts` (1), `install-url.test.ts` (1). test-review S1.1 + S3.1; the standing override covers the throws inside the _helpers_, not inline branches → add the `expectPluginOk`/`expectPluginRejected` pair (and the parse-result pair) — fixed (`754c35e95`): the plugin-level pair in `__tests__/fixtures/load-plugin.ts` beside the component pairs, `expectInstallOk` in the CLI fixture module, the parse row a local `expectSourceOk`
- T2 `plugin-name.test.ts` — no docstrings at all, on either `test.each` block (test-review S2.1) — fixed (`debff9491`)
- T3 `load-plugin-manifest.test.ts` — the first row has no docstring while every sibling does (test-review S2.1) — fixed (`5eb50f4a0`)
- T4 rows over the 10-line density cap because they inline `$schema`/`name` instead of the purpose-built `makeManifestRoot(extraFields)`: `extensions.test.ts` (3 rows), `top-level-fields.test.ts` (3), `load-plugin.test.ts` (test-review S1.4) — fixed (`e8433b4c9`): the rows build through the fixture
- T5 `'minimal-plugin'` repeated as a raw literal where the house form is a named constant (test-review S2.4)
- T6 rows probing `reports[0]` where F3's `expectReportAbout` fits: `top-level-fields.test.ts`, `skills.test.ts`, `mcp-config.test.ts` (2 rows), `load-plugin.test.ts` (test-review S5, clean-code S5.2)
- T7 `plugin-install.test.ts` — the success row runs ~25 non-blank lines and six assertions, mixing the result contract with the on-disk outcome (test-review S1.4) — fixed (`952982cd2`): one row per observable (the result contract / the on-disk outcome) over a shared `installStandardPlugin` setup; sabotage-proven — removing the data-dir provisioning turns only the disk row red
- T8 four CLI test files duplicate verbatim: the temp-root registry + `afterEach`, `PLUGIN_JSON`, `SKILL_MD`, `MCP_JSON`, `makeTarball` (2 files) and `writePluginRoot` (2 files) (test-review S6.1, test-files organization) → one shared fixture module — fixed (`d50d6fb4c`): `cli/src/__tests__/helpers/plugin-fixtures.ts`, and the temp prefix names the test file it came from
- T9 three command rows name "exits nonzero"/"exits zero" in their docstrings but never assert `process.exitCode` — the claimed behavior is unasserted (test-review S5.1) — fixed (`db0c18c9b`): the runner helper reports the status a shell would see; bun keeps the last `process.exitCode` it was given (`= undefined` is ignored), so the reset writes 0 and both capture and code are restored between rows; sabotage-proven — dropping the nonzero exit turns exactly the two rows that claim it red
- T10 `plugin-child-process.test.ts` — `mkdtempSync(..., 'plugin-t34-test-')` leaks the internal test number into shipped test code; the earlier internal-reference sweep missed the prefix (the no-internal-references lesson) — fixed (`37dad972a`): the prefix names the test file, not a test number

## Missing operations (null versions)

- ✅ loadManifest(root) — no longer a null version: Phase 1 landed it (T1–T11), so it is what the phase was missing. Its scope is the manifest alone — components, MCP, expansion and install are still null, which is what the rest of this list records
- ✅ loadPlugin(root) Service does not exist — landed with the Phase 2 discovery seam (T29: `plugin-discovery.ts`, `loadPlugin` per directory)
- ✅ spec→freebuff MCP server mapping does not exist — landed with Phase 3 (T26: the adapter in `plugins/mcp-config.ts`)
- placeholder expansion does not exist — deferred with Phase 4 to the next PR, not a Phase-5 dependency for a remote-only plugin
- ✅ plugin install <url> CLI command does not exist — landed with Phase 5 (T32: `plugin-install-command.ts`, wired through `cli-args.ts` + `index.tsx`)
- ✅ the plugins root (`~/.agents/plugins`) and the `.data` root are undefined and nothing scans them — landed with Phase 5 (T29/T30: `plugins-root.ts` + `plugin-discovery.ts`; T32 provisions `.data`)

## Tracking (agreed 2026-09-14)

- this file is the phase tracker: per-test ✅ markers applied as tests graduate, pruned at phase close
- working files (this file + backlog.md) ride **committed on the feature branch** — versioned with the work, restored to the driver branch before any PR
- the leak rule (check.ts) therefore fires on _pushed_ PR-bound branches, not on local branches mid-work
- delete this section at promotion time along with the files themselves (strip = `git rebase --onto origin/main` + restore on driver)
- strip before the PR: the local formatter hook (`scripts/hooks/pre-commit` plus the local `core.hooksPath` setting) — prettier is declared upstream but never run there, so this hook is our tooling and not theirs
- docstrings tiered by visibility: public-facing declarations get one, full stop (duty, not taste — full contract: purpose, failure modes, invariants, spec citations); private helpers optional by default, earning one only for whys the code cannot express (esoteric/paper algorithm → explain + link source; convoluted bug workaround → explain + link issue) — otherwise comment blocks bury the code and break the file-size/density criteria. Content governed everywhere: ubiquitous language, no textbook dialect ("first-class", "composed assertion"), no restating the name — enrich with the contract instead. Inline comments stay the grimace category (extraction signal) — handbook Clean Code Ch 5. Production-only tiering: test fixtures stay docstring-mandatory (test-review Stage 2 Item 1). Audience = fellow programmers & the curious domain expert
- every test is audited against all three checklists — clean-code (6 stages), clean-architecture (5), test-review (6) — **at the conclusion of the test and before its code commit**, with every stage read fresh rather than recalled, and the verdict plus any override written into the Audit record below; strictly, no exceptions (human ruling 2026-09-14, restated the same day after a pass was found to have skipped two clean-architecture stages). A test is not finished until that row exists. Phase close adds the full audit plus the lessons index

- skill-registry takes the plugin root as a third source; the user's and project's own roots untouched
- one adapter module maps spec server variants → freebuff mcpConfig shapes
- loader returns { installed: InstalledPlugin, reports } and the CLI renders reports; no logging from common/

## Audit record (human ruling 2026-09-14: hand-walk all 17 stages fresh per change)

Stages per domain: clean-code 6, clean-architecture 5, test-review 6. A verdict is the count of stages passed. Every override names the item it exempts, per the checklists' own traceability rule — an override is a pass under a named exception, never a silent one.

| Scope                                                                                                                                                                             | clean-code               | clean-architecture             | test-review              | Findings                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ | ------------------------------ | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T8 §5.4 metadata, re-audited in the retro pass                                                                                                                                    | 6/6                      | 5/5                            | 6/6                      | A5 (two accumulators in `metadata.ts`) fixed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| T9 §5.2 parse and top-level shape, re-audited                                                                                                                                     | 6/6                      | 5/5                            | 6/6                      | A1 (a conditional inside a test body) fixed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| T10 §4.1.1 containment                                                                                                                                                            | 6/6                      | 5/5                            | 6/6                      | A3 override                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| T11 §5.1 missing manifest                                                                                                                                                         | 6/6                      | 5/5                            | 6/6                      | none — sabotage: removing the read's `catch` turns this row and only this row red                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| T13/T16 skills component (plugins/skills.ts)                                                                                                                                      | 6/6                      | 5/5                            | 6/6                      | one clean-arch S1.4 finding (the walk mutated the reader's returned map to remove a refused skill) fixed before recording — the map is now filtered, not mutated; override below for the fs use; sabotage proofs: 5 run, each turning exactly its row red (S1 hit the wrapper instead of a decision and was discarded)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| T21/T18/T20/T26 + §4.1.1 row, MCP component (plugins/mcp-config.ts)                                                                                                               | 6/6                      | 5/5                            | 6/6                      | three findings, all fixed before recording: (1) clean-arch S1.4 — server/report accumulation in a loop, now the immutable reduce form (the A5 precedent, third occurrence of this lesson); (2) test rows carried multi-line inline JSON literals over the density cap, extracted to named fixture constants; (3) a design inconsistency — wrong-kind mcp.json answered with silent absence while §6.2 makes it component-invalid, honored here because (unlike skills) the walk is entirely ours; the audit also caught, via the first sabotage run, that §4.1.1 was checked after kind classification, letting an escaping junction hide behind the wrong-kind branch — reordered, and the escape row added. Sabotage proofs: mapping swap (T26 red, now the sole mapping assertion), kind-classification removal (both §6.2 rows red), §4.1.1 removal (escape row red). T21 dropped as its own row after review — subsumed by T26                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| T1–T7 re-audit — CA stages 04 (components) + 05 (architecture), both read fresh and applied 2026-09-15, scoped to the plugin component (`common/src/plugins/**`) vs `origin/main` | n/a — CA-stage debt only | 5/5                            | n/a — CA-stage debt only | **These two stages found no violations** (the wider per-file walk that followed found P1–P5 and T1–T10 — see the full-audit section below). ADP: the component graph is strictly layered (contracts → per-§ rules → use cases → service) with zero back edges — verified that `common/{util/path,types/mcp,types/skill,constants/skills}` import nothing from `plugins`. SDP: every edge points from the composing modules toward the leaf contracts; two numeric observations recorded as benign rather than fixed — the tiny concrete predicates (`json-value`, `plugin-name`, `schema-version`) land numerically in the zone of pain because they are concrete with fan-in and no abstraction, which is the checklist's own stdlib-style exception (their contracts are fixed by the spec), and `containment` (I≈0.5) is depended on by `load-plugin-manifest` (I≈0.09), a strict reversal that is an artifact of applying a component-level instrument to modules inside a single component. SAP: the most-depended modules are the pure ones (`manifest/plugin-manifest` types-only, `report` a single interface). CCP: one considered judgment — `install-url.ts`'s change driver is the CLI's input surface (GitHub URL forms), not the spec, unlike its neighbours; kept in the component for REP (one release unit, its tests and fixtures together), with relocation to the CLI recorded as the thing to revisit if a second consumer ever appears (the in-session `/plugin` command, already out of scope). CRP: external consumers deep-import exactly what they need — `common/plugins/load-plugin` (2), `install-url` (1), `report` (1, type-only) — with no barrel and no `index.ts`, so nobody is forced onto the MCP adapter or the skills walk; noted that discovery imports the composite `load-plugin`, so a skills-only registry init also reads `mcp.json` — benign (same entity, same manifest gate), with `loadPluginSkills`/`loadPluginMCP` already exported if a skills-only consumer appears. Stage 5, all five applicable criteria pass: **the dependency rule** — zero outward imports from the component (only `node:fs`/`node:path`, common's own lower modules, and `zod` inherited from the existing `types/mcp` schemas), the CLI's plugin modules import inward, and nothing in common imports the CLI; **policy vs detail** — parsing, validation and reporting in common, argv/console/fetch/archive extraction in the CLI; **deferring options** — the fetch is injected, `Bun.Archive` is the runtime rather than a framework, and MCP server shapes reuse the existing schemas instead of re-declaring; **screaming architecture** — `plugins/` and `manifest/` are named for the domain and the spec sections, the CLI surfaces for the use case; **naked testability** — the rule modules are pure and directly testable, the filesystem use case stays thin, and no test needs a UI, server or network. The orchestration-adapter exemption (criterion 6) was not invoked: no module claims that role and no vendor SDK is bound inside the core |
| T31 install URL parse (plugins/install-url.ts + test)                                                                                                                             | 6/6                      | 5/5                            | 6/6                      | audit ran late (written before the per-test audit rule was re-restated); one finding, caught by the human challenge of 2026-09-15 and fixed: `parsePluginSourceUrl` at 66 lines mixed boundary validation with path carving across four copies of the rejection literal — now `parseRepoPath` (a 23-line orchestrator), a named splitter for the path segments, and a `rejected()` constructor for the failure shape. Test rows table-driven with named `form`/`case` labels. Sabotage proof: removing the `tree` branch turns exactly the two tree rows red                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| T32/T33 install pipeline (cli/commands/plugin-install.ts + test)                                                                                                                  | 6/6                      | 5/5                            | 6/6                      | findings fixed before recording: (1) the abort rows asserted the plugins root did not exist, but the test's own `mkdtempSync` created it — assertions now check `readdirSync(pluginsRoot)` is empty, which is the actual contract (nothing written into the root); (2) the fetch seam typed `typeof fetch` forced unsound casts (bun-types adds `preconnect`) — narrowed to `(url) => Promise<Response>`, what the pipeline uses; (3) the data dir was provisioned with a `.gitkeep` file the docstring never mentioned — now the docstring's one plain `mkdirSync`. Sabotage proof: disabling the conflict guard turns exactly the two collision rows red (the already-installed row stays green — a different guard, independent coverage). Fourth finding added after recording (human challenge 2026-09-15): the S2 pass was too lenient — `handlePluginInstall` at 97 lines mixed orchestration with inline error templating; download/extract now live in `downloadPluginSource` (29 lines) and the orchestrator is a linear sequence of named steps. Lesson recorded: S2's criterion is level-mixing, not raw length — length only flags where to look                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| test/setup-scm-loader.ts (comment-only rework)                                                                                                                                    | 6/6                      | n/a — not architecture-bearing | n/a — no test file       | docstring shortened per the comments lesson: the load-bearing fact first, mechanism in two lines, history dropped                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

Overrides:

- **test-review Stage 3 item 1** — the two `throw`s inside `expectManifestOk` / `expectManifestRejected`. Those are assertion helpers, not tests, and TypeScript cannot narrow from an `expect` call, so the throw is the narrowing device; the alternatives are a cast or an assertion too weak to prove the branch. No `*.test.ts` in the component contains a conditional.
- **clean-architecture Stage 3 item 1, Stage 5 item 2** — `plugins/containment.ts` imports `node:fs`. §4.1.1 defines the rule over filesystem-resolved paths, and `util/path.ts` delegates resolution to callers by its own docstring, so a pure predicate would push the resolve-first duty onto every Phase 2/3 call site — the bug T10 exists to prevent. Accepted 2026-09-14.
- **clean-architecture Stage 4 item 4 (CCP)** — the containment decision spans `util/path.ts` (compare) and `plugins/containment.ts` (resolve + rule). Justified: the util's other consumer compares lexically on purpose (`project-file-tree.ts:317`), so the two halves change for different reasons (REP/CRP).
- **test-review Stage 3 item 3** — tests drive a real filesystem. §5.1's contract is a manifest at a path, `readPluginJson` is the Humble Object that keeps that boundary thin (clean-code Stage 5 item 4), and Stage 4 item 2 names the filesystem among the genuine external boundaries mocks are reserved for.
- **clean-architecture Stage 3 item 1 (DIP)** — `plugins/skills.ts` imports `node:fs` (readdir/stat) for discovery, beside the injected reader. §4.1.1's boundary 3 needs the candidates enumerated _before_ delegation to know which to exclude, and the reader's own docstring pins discovery-by-directory as its contract; injecting an enumerator too would double the seam to trade one import for two parameters. Accepted 2026-09-14 — the alternative (a second injected function per §4.1.1 boundary) is recorded as the first thing to revisit if Phase 3 wants the same shape for `mcp.json`.
- **Windows fixture note (test-review Stage 2)** — junctions cannot point at a file (probed: the junction is created but every stat/read on it then throws ENOENT), so T16's per-skill escape fixture makes the skill _directory_ the reparse point, with a real SKILL.md inside its target — the honest escape, since the reader would load that skill from outside the root.
